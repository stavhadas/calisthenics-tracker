services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/db.sqlite
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - PORT=3000
    restart: unless-stopped
